package sns.controller;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class IndexController {
	/*
	 * public IndexController(HttpServletRequest request , HttpServletResponse
	 * response , String[] comments) throws ServletException, IOException {
	 * 
	 * if(comments[comments.length-1].equals("index.do")) { index(request,response);
	 * } }
	 * 
	 * public void index(HttpServletRequest request , HttpServletResponse response)
	 * throws ServletException, IOException {
	 * request.getRequestDispatcher(request.getContextPath()).forward(request,
	 * response); }
	 */
}
